domain: eliace.github.io       # if you want to force HTTPS, specify the domain without the http at the start, e.g. example.com
url: https://eliace.github.io/chorda3/  # the base hostname and protocol for your site, e.g. http://example.com
baseurl: /packages/chorda-docs/docs/      # place folder name if the site is served in a subfolder